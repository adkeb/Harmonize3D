
---

# Local 3D‑to‑AI Image Rendering Workflow Plan

## 1. Overview

构建一个 **完全本地可运行的 3D → AI 图像渲染流水线**，包括：

1. 文生或参考图生成 3D 模型
2. Blender 渲染多角度通道图（RGB、Depth、Edge、Normal、Mask）
3. 使用本地高质量图像生成模型（无 ComfyUI）进行风格渲染和一致性输出
4. 自动筛选并产出最终图像资产

目标：

* **本地运行，无网络依赖**
* **RTX 5090（32 GB VRAM）可支持**
* 使用**当前顶级开源生成模型**，具有高 prompt 遵从度和输出一致性
* 自动化、可脚本化执行

---

## 2. Recommended Models (2026)

根据最新行业评测和对比结果，目前最适合本地高一致性图像生成的模型包括：

* **FLUX.1 系列**（Black Forest Labs）

  * **FLUX.1 Dev**：最高质量、最强 prompt 遵从度（非商业版权重）
  * **FLUX.1 Schnell**：快速 4‑step 推理、Apache 2.0 商业权限开源版本
  * 优势：在开放权重排行榜中整体领先 Stable Diffusion 系列，在 prompt 遵守、图像质量上表现优异([Presenc AI][1])

* **Stable Diffusion 3.5 Large**（Stability AI）

  * 强生态支持、广泛的 LoRA/控制模块资源，可作为实用备用模型
  * 输出质量略低于 FLUX 但更成熟的社区支持([Presenc AI][1])

> 备注：FLUX.1 Pro 为商业闭源更高质量版本，可根据许可证策略自行决定是否引入。

---

## 3. Workflow Architecture

整体工作流如下图所示：

```
Text/Ref Image → 3D Model Gen → Multi‑View Blender Renders
     ↓
3D Rendering Channels (RGB, Depth, Edge, Normals, Mask)
     ↓
Scripted AI Image Generation (No UI)
     ↓
Consistency Check + Ranking
     ↓
Final Output Images / Assets
```

---

## 4. Implementation Steps

### 4.1 3D Model Generation

1. 输入：Prompt 或参考图
2. 使用图生 3D 工具生成 3D 模型（`.glb/.obj` 格式）：

   * 推荐：Hunyuan3D 或 TRELLIS / StableFast3D 等
   * 输出带纹理或先无纹理再用图像模型优化

---

### 4.2 Blender Batch Renders

目的：为图像模型提供一致的几何控制通道

每个模型导入 Blender 后自动渲染以下图层：

| 通道               | 用途     |
| ---------------- | ------ |
| RGB.png          | 主图     |
| Depth.png        | 深度控制   |
| Edge/Outline.png | 结构轮廓   |
| Normal.png       | 表面法线方向 |
| Mask.png         | 前景遮罩   |

**Blender Script 自动化示例**：

```python
# batch_render.py (Blender Python)
import bpy, sys

# load args
glb_path = sys.argv[-2]
output_dir = sys.argv[-1]

bpy.ops.import_scene.gltf(filepath=glb_path)

# configure passes
scene = bpy.context.scene
scene.use_nodes = True
tree = scene.node_tree
# set up layers: depth, normals, etc.
tree.nodes["File Output"].base_path = output_dir

# define multiple cameras
# render RGB, Depth, Normal, Edge, Mask
...
bpy.ops.render.render(animation=False)
```

---

### 4.3 Scripted AI Image Generation

目标：将 Blender 渲染图输入模型，生成一致性高、风格清晰的最终图像

#### 4.3.1 Model Loading

使用本地推理框架（如 Diffusers + PyTorch + TensorRT / ONNX）来加载权重：

```python
from diffusers import DiffusionPipeline

model = DiffusionPipeline.from_pretrained(
    "black-forest-labs/FLUX.1‑dev",
    torch_dtype=torch.float16
).to("cuda:0")
```

对于 FLUX.1 Schnell 用 Apache2.0 权限版本：

```python
model = DiffusionPipeline.from_pretrained(
    "black-forest-labs/FLUX.1‑schnell",
    torch_dtype=torch.float16
).to("cuda:0")
```

---

#### 4.3.2 Prompt System

定义统一 prompt template，例如：

```
主体: <object description>
View: <camera angle identifier>
Style: photorealistic studio lighting, sharp focus
Material: <e.g., metal surface, soft shadows>
```

多个通道图组合输入作为约束条件。

---

### 4.4 Consistency Check & Ranking

目的：从多次生成结果中挑选几何和风格一致性最佳的图像

判断逻辑可参考：

* 对比 edge / depth 是否与原 3D 渲染一致
* 结构相似度阈值筛选
* 文字提示一致性衡量

可用 CLIP 或专用结构一致性网络打分。

---

## 5. Output Targets

最终输出：

| Output Type    | 用途                              |
| -------------- | ------------------------------- |
| High‑res PNGs  | 产品图、展示图                         |
| Variants       | 不同风格/灯光                         |
| 360° Turntable | 线序列或视频                          |
| Metadata       | prompt / seed / hyperparameters |

---

## 6. Deployment Considerations

* **Local Runtime Optimization**

  * 使用 TensorRT / ONNX for faster inference
  * 需要手动控制显存约束，RTX 5090 非常适合高质量模型

* **Licensing**

  * FLUX.1 Dev：非商业权重
  * FLUX.1 Schnell：Apache 2.0 商业许可
  * Stable Diffusion 3.5 Large：Community License（确认商业边界）([Presenc AI][1])

---

## 7. Example CLI Script Snippet

```bash
# 1. Generate 3D model from prompt
python generate_3d.py --input "cyberpunk fox figurine"

# 2. Blender batch render
blender --background --python batch_render.py -- my_model.glb ./renders/

# 3. AI image generation (FLUX.1 Schnell)
python run_ai_render.py --model flux.1-schnell --input_renders ./renders/ --output final/

# 4. Consistency filter
python score_consistency.py --input final/ --report report.json
```

---

## 8. Notes & Best Practices

* FLUX.1 系列在 prompt 遵循度和一致性方面表现优于很多其他开源模型，是 2026 年的首选开源图像生成基础权重([Presenc AI][1])
* Stable Diffusion 3.5 Large 保留了 SD 系列丰富的生态工具链，可作为备用方案([SingularityByte AI Magazine][2])
* 避免单次生成出现形状偏差，可用“多次采样 + 一致性打分”增强稳定性

---

[1]: https://presenc.ai/research/best-open-weight-image-generation-models-2026?utm_source=chatgpt.com "Best Open-Weight Image Generation Models 2026 | Presenc AI"
[2]: https://singularitybyte.com/tutorials/best-self-hosted-ai-image-generator-2026.html?utm_source=chatgpt.com "Best Self-Hosted AI Image Generator in 2026: Flux, SDXL, and HiDream-L1 Compared | SingularityByte AI Magazine"
